import { cookies } from "next/headers";import { redirect } from "next/navigation";export const STUDENT_COOKIE="papel_student_id";export const ADMIN_COOKIE="papel_admin_session";export function getStudentId(){return cookies().get(STUDENT_COOKIE)?.value??null}export function requireStudentId(){const id=getStudentId();if(!id) redirect("/");return id}export function setStudentId(id:string){cookies().set(STUDENT_COOKIE,id,{httpOnly:true,sameSite:"lax",secure:process.env.NODE_ENV==="production",path:"/",maxAge:60*60*24*180})}export function clearStudentId(){cookies().delete(STUDENT_COOKIE)}export function setAdminSession(){cookies().set(ADMIN_COOKIE,process.env.ADMIN_SESSION_TOKEN||"dev-admin-session",{httpOnly:true,sameSite:"lax",secure:process.env.NODE_ENV==="production",path:"/",maxAge:60*60*12})}export function requireAdmin(){const t=cookies().get(ADMIN_COOKIE)?.value;const e=process.env.ADMIN_SESSION_TOKEN||"dev-admin-session";if(!t||t!==e) redirect("/admin/login")}export function clearAdminSession(){cookies().delete(ADMIN_COOKIE)}