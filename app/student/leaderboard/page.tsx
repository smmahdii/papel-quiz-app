import { StudentNav } from "@/components/StudentNav";import { requireStudentId } from "@/lib/auth";import { supabaseAdmin } from "@/lib/supabase";import { bnNumber } from "@/lib/utils";export default async function Page(){requireStudentId();const sb=supabaseAdmin();const {data:q}=await sb.from('quizzes').select('id,title').eq('status','published').order('created_at',{ascending:false}).limit(1).maybeSingle();const {data:rows}=q?await sb.from('attempts').select('*,profiles(name)').eq('quiz_id',q.id).order('score',{ascending:false}):{data:[]};return <><StudentNav/><main className="mx-auto max-w-6xl px-4 py-6"><section className="hero"><h1 className="text-3xl font-black">Leaderboard</h1><p>{q?.title}</p></section><div className="mt-5 card"><table className="w-full text-left text-sm"><tbody>{(rows||[]).map((r:any,i)=><tr key={r.id} className="border-t"><td className="p-3 font-black">{bnNumber(i+1)}</td><td className="p-3">{r.profiles?.name}</td><td className="p-3">{bnNumber(r.score)} / {bnNumber(r.total_marks)}</td></tr>)}</tbody></table></div></main></>}