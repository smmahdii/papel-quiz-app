import Link from "next/link";import { StudentNav } from "@/components/StudentNav";import { requireStudentId } from "@/lib/auth";import { supabaseAdmin } from "@/lib/supabase";import { bnNumber } from "@/lib/utils";export default async function Page(){const sid=requireStudentId();const {data}=await supabaseAdmin().from('attempts').select('*,quizzes(title)').eq('student_id',sid).order('created_at',{ascending:false});return <><StudentNav/><main className="mx-auto max-w-6xl px-4 py-6"><section className="hero"><h1 className="text-3xl font-black">আমার রেজাল্ট</h1></section><div className="mt-5 card overflow-x-auto"><table className="w-full text-left text-sm"><tbody>{(data||[]).map((a:any)=><tr key={a.id} className="border-t"><td className="p-3 font-bold">{a.quizzes?.title}</td><td className="p-3">{bnNumber(a.score)} / {bnNumber(a.total_marks)}</td><td className="p-3"><Link href={`/student/result/${a.id}`} className="btn-secondary py-2">Review</Link></td></tr>)}</tbody></table></div></main></>}