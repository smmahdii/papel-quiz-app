import { AdminNav } from "@/components/AdminNav";import { StatCard } from "@/components/StatCard";import { requireAdmin } from "@/lib/auth";import { supabaseAdmin } from "@/lib/supabase";export default async function Page(){requireAdmin();const sb=supabaseAdmin();const [s,q,qs,a]=await Promise.all([sb.from('profiles').select('id',{count:'exact',head:true}).eq('role','student'),sb.from('quizzes').select('id',{count:'exact',head:true}),sb.from('questions').select('id',{count:'exact',head:true}),sb.from('attempts').select('id',{count:'exact',head:true})]);return <><AdminNav/><main className="mx-auto max-w-7xl px-4 py-6"><section className="hero"><h1 className="text-3xl font-black">Admin Dashboard</h1></section><div className="mt-6 grid gap-4 sm:grid-cols-2 lg:grid-cols-4"><StatCard label="Students" value={s.count||0}/><StatCard label="Quizzes" value={q.count||0}/><StatCard label="MCQ" value={qs.count||0}/><StatCard label="Attempts" value={a.count||0}/></div></main></>}