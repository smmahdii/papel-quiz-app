import { AdminNav } from "@/components/AdminNav";import { requireAdmin } from "@/lib/auth";import { supabaseAdmin } from "@/lib/supabase";export default async function Page(){requireAdmin();const {data}=await supabaseAdmin().from('attempts').select('*,profiles(name,phone),quizzes(title)').order('created_at',{ascending:false});return <><AdminNav/><main className="mx-auto max-w-7xl px-4 py-6"><section className="hero"><h1 className="text-3xl font-black">Result Report</h1></section><div className="card mt-5">{(data||[]).map((a:any)=><div key={a.id} className="border-t py-3"><b>{a.profiles?.name}</b> — {a.quizzes?.title} — {a.score}/{a.total_marks}</div>)}</div></main></>}