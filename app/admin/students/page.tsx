import { AdminNav } from "@/components/AdminNav";import { requireAdmin } from "@/lib/auth";import { supabaseAdmin } from "@/lib/supabase";export default async function Page(){requireAdmin();const {data}=await supabaseAdmin().from('profiles').select('*').eq('role','student').order('created_at',{ascending:false});return <><AdminNav/><main className="mx-auto max-w-7xl px-4 py-6"><section className="hero"><h1 className="text-3xl font-black">Students</h1></section><div className="card mt-5">{(data||[]).map((s:any)=><div key={s.id} className="border-t py-3"><b>{s.name}</b> — {s.phone} — Roll: {s.admission_roll} — Batch: {s.batch_no}</div>)}</div></main></>}